<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= @movie["title"] %> - Guardalo!</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <a href="/" class="btn btn-primary mb-3">⬅ Torna alla Home</a>

        <div class="row">
            <div class="col-md-4">
                <img src="https://image.tmdb.org/t/p/w500<%= @movie["poster_path"] %>" class="img-fluid rounded">
            </div>
            <div class="col-md-8">
                <h1><%= @movie["title"] %></h1>
                <p><strong>Data di uscita:</strong> <%= @movie["release_date"] %></p>
                <p><strong>Voto medio:</strong> <%= @movie["vote_average"] %>/10</p>
                <p><%= @movie["overview"] %></p>
                
                <h3>Disponibile su:</h3>
                <ul>
                    <% if @movie["watch/providers"] && @movie["watch/providers"]["results"]["IT"] %>
                        <% @movie["watch/providers"]["results"]["IT"]["flatrate"]&.each do |provider| %>
                            <li><%= provider["provider_name"] %></li>
                        <% end %>
                    <% else %>
                        <li>Non disponibile in streaming</li>
                    <% end %>
                </ul>
            </div>
        </div>
    </div>
</body>
</html>

